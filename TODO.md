# TODO List

## 이름 정규화 (Normalization) & 사용자 별칭 관리

이름이 "YOSEP PARK"과 "박요셉"처럼 파편화되어 보이는 문제를 해결하기 위해, 사용자가 직접 대표 이름과 별칭을 정의하고 관리할 수 있도록 개선합니다.

- [ ] **DB 스키마 확장 (Multi-tenant Alias Management)**
  - `user_aliases` 테이블을 사용자(Tenant)별로 독립적으로 관리할 수 있도록 구조 개선
  - 서비스 비가입 사용자(연락처/동료)에 대해서도 별칭을 등록할 수 있는 `contacts` 테이블 검토
- [ ] **백엔드 로직 고도화**
  - `NormalizeName` 함수가 현재 사용자(Tenant)의 전용 별칭 데이터를 참조하도록 수정
  - 대소문자, 공백 등 느슨한(Fuzzy) 일치 처리 추가
- [ ] **프론트엔드 UI 구현 (Alias Management UI)**
  - 사용자 설정 페이지 내 '이름 관리' 또는 '별칭 설정' 섹션 추가
  - 대표 이름 입력 -> 별칭(콤마로 구분 등) 추가 기능
  - 기존의 파편화된 이름들을 제안(Suggestion)하고 하나로 묶어주는 편리한 UX 제공
- [ ] **기존 데이터 마이그레이션**
  - 별칭이 등록된 후, 기존에 저장된 태스크들의 이름을 일괄적으로 정규화 처리하는 기능 제공

---

## 기타 개선 사항

- [ ] Gemini 토큰 사용량 모니터링 및 최적화 지속
- [ ] VPS 배포 프로세스 자동화 고도화
